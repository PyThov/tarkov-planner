# Tarkov Planner

Cloning this repo:
```bash
cd tarkov-planner && git submodule init && git submodule update
```

Run with:
```bash
docker-compose up --build -d
```

and go to http://localhost:3000 in your preferred browser
